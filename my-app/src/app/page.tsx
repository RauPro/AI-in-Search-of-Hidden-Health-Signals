import HealthIntakeForm from "@/components/HealthIntakeForm";

export default function Home() {
  return (
    <div className="min-h-screen w-full bg-cream px-4 py-8 sm:py-12">
      <HealthIntakeForm />
    </div>
  );
}
